apiVersion: v1
kind: Service
metadata:
  name: smarthome-backend-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: smarthome-backend
  ports:
    - name: HTTP
      protocol: TCP
      port: 3000
      targetPort: 3000
